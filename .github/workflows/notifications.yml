name: Telegram Notifications
on:
  push:
    branches:
      - main
jobs:
  notify:
    name: Send message to Telegram
    runs-on: ubuntu-latest

    defaults:
      run:
        shell: bash

    steps:
      - name: Format Telegram Message
        env:
          mustache_template: |
            {{#repository}}
            📣 ОБНОВЛЕНИЯ В <b><a href="{{{repository.html_url}}}">{{{repository.full_name}}}</a></b>

            {{/repository}}
            {{#commits}}
            <strong><a href="{{{url}}}">✔️ {{id}}</a></strong>
            <em>({{timestamp}})</em> by <strong><a href="https://github.com/{{author.username}}">👤{{author.username}}</a></strong>

            <pre>{{message}}
            </pre>
            {{/commits}}
            {{^commits}}No commits found... :({{/commits}}
        run: |
          echo "::group::Create Mustache template & view files"
          echo '${{ toJSON(github.event) }}' > view.json
          echo '${{ env.mustache_template }}' > template.mustache
          echo "::endgroup::"
          npx -q mustache@4.2 view.json template.mustache > __out__
          echo "::debug::$(cat -- __out__)"

      - name: Send formatted message
        run: |
          curl -q -s -L --fail-with-body --max-time 10 --retry 5 --retry-delay 2 --retry-max-time 10 \
            -d "disable_web_page_preview=false" -d "parse_mode=HTML" \
            -d "chat_id=${{secrets.TELEGRAM_TO}}" --data-urlencode "text@"<(cat -- __out__) \
            "https://api.telegram.org/bot${{secrets.TELEGRAM_TOKEN}}/sendMessage" > result.json || true
          echo "::debug::$(jq '.' result.json 2>/dev/null || cat result.json)"

      - name: Check Telegram response
        run: |
          [[ $(jq '.ok' result.json 2>/dev/null) == "true" ]] || ( printf "::error::\
          Telegram API response: %s - %s\n" \
          $(jq '.error_code,.description' result.json 2>/dev/null || echo '<Not JSON!>'); \
          exit 1 )
